@page
@model IndexModel
@{
    // Redirects the root path ("/") to the Dashboard page.
    // If you want to display content here, you would implement the index logic.
    Response.Redirect("/Dashboard");
}